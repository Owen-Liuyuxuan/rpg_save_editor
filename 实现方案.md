# 魔法少女シルフィーナ：Windows 存档修改器实现方案

## 已完成的本地验证

日期：2026-09-12。实际游戏目录为 `C:\Users\YuxuanLiu\Downloads\otomi-games.com_GN6UASU1O\魔法少女シルフィーナ\www`。

本地 `js/rpg_core.js:183` 确认引擎为 MV 1.6.1；`js/rpg_managers.js:674`、`:691` 确认压缩和解压使用 LZString Base64，`:755` 确认 save 目录规则。data 中数据库是可直接解析的 JSON。

运行环境已有 Node.js v24.19.0。本次直接复用游戏自带 `js/libs/lz-string.js`，无需安装额外 Python 包或下载在线修改器。probe.cjs 在独立 VM context 中运行该压缩库，未运行游戏和插件脚本；VM 不是面向恶意代码的安全沙箱，正式产品必须内置审核后的压缩库，不能执行用户选择的任意游戏脚本。具体库文件 SHA-256 已记录在 report.json 中。

| 文件 | 金币 | 地图 ID | 测试 |
|---|---:|---:|---|
| file1.rpgsave | 1000 | 16 | 通过 |
| file2.rpgsave | 2470 | 2 | 通过 |
| file3.rpgsave | 3620 | 37 | 通过 |
| file4.rpgsave | 3840 | 111 | 通过 |
| file5 - 副本.rpgsave | 3840 | 43 | 通过 |
| file5.rpgsave | 9500 | 5 | 通过 |
| file6.rpgsave | 9500 | 2 | 通过 |

7 个游戏存档及 config/global 共 9 个文件均通过解压、JSON 解析、原始 JSON 重压缩一致性、JSON 序列化往返测试。对每个游戏存档仅将金币加 1，保存到实验输出目录，回读后恢复金币再与原始对象深度比较，均无其他字段变化。逐文件原始 SHA-256 前后相同。以上是格式和数据完整性测试，尚未做游戏内加载验收，不能据此承诺所有改值均符合游戏逻辑。

数据库索引包含 3 个角色记录、214 个地图记录、506 个有名称的变量和 948 个有名称的开关。Items 有 1500 个非空对象记录、Skills 有 500 个；其中可能含空名称占位项，GUI 应默认过滤空名称但保留按 ID 查询能力。

## 格式与编辑原则

处理链：rpgsave 文本 → LZString.decompressFromBase64 → JSON.parse → 保留元数据的原始树 → 指定字段补丁 → JSON.stringify → LZString.compressToBase64。

原始存档不是只包含玩家数值的普通 JSON。MV 的 JsonEx 在本游戏中使用 `@`（类名）、`@c`（对象编号）、`@a`（数组内容）和 `@r`（共享或循环引用）。原始树是唯一写出来源；GUI 建立展示投影并记录实际路径，不重新组装整个存档，也不通过 JsonEx.parse 清除元数据后直接 JSON.stringify。引用节点通过对象编号映射展示，初版禁止改变引用拓扑；新增简单物品数量可以增加字典中的数字键，新增复杂 Game_Item 对象推迟到适配完成。

| GUI 内容 | 原始字段/来源 | 规则 |
|---|---|---|
| 金币 | party._gold | 整数，按引擎/插件上限校验 |
| 道具/武器/防具 | party._items / _weapons / _armors | ID 对接各自数据库，数量整数 |
| 角色 | actors._data（可能由 @a 包装） | 显示姓名、等级、HP、MP；初版谨慎开放修改 |
| 变量 | variables._data | System.variables 提供名称；保留数字/字符串等真实类型 |
| 开关 | switches._data | System.switches 提供名称；布尔值；缺失项显示默认 false |
| 自开关 | selfSwitches._data | 地图 ID、事件 ID、字母；高级页只读起步 |
| 地图 | map._mapId、player | MapInfos 提供名称；初版只读 |
| 插件字段 | 如 oswDatas | 保留全部未知字段，树形查看 |
| 存档列表 | global.rpgsave | 槽位标题、时间、头像等元数据 |

数组索引必须保留，包括 null 占位。变量缺失项通常读取为 0，但不要把原始字符串强制转换成数字。标记键必须从物品数量列表中排除。未改动的原始树字段及元数据完全保留。

## 本游戏适配

启用了 140 个插件，不能假设 MV 默认游戏规则全部适用。已确认：

- VariableLimitation：加载存档后执行 applyLimitations。插件参数配置了 34 个变量限制，例如变量 35 为 0–150、37 为 0–200、50 为 -100–100。GUI 读取 plugins.js 中的 JSON 参数生成校验，不执行插件。
- setItemMax：物品最大数量可受数据库备注 itemMaxNum 和运行时动态上限影响，不能统一硬编码为 99。实现时读取完整规则，解析优先级并显示规则来源。
- FTKR_OriginalSceneWindow：通过 makeSaveContents 添加 oswDatas，写出必须保留。
- DynamicEquipParam、TMEquipSlotEx 等使装备和能力计算需要另行适配。等级并非孤立值，经验、技能学习和刷新逻辑有关；初版将等级/装备作为查看项，后续实现一致性操作。

变量名不等于语义保证。比如名为角色 HP 的变量可能是显示缓存，与 actors 内真实 HP 不同。提供“存档差异比较”帮助识别变化；对于剧情跳过、解锁等操作，需再检查 CommonEvents 和 Map 事件的读写关系，不能仅凭名称生成一键全开预设。

## Windows GUI 架构

建议采用 Electron + TypeScript + React，同时支持 MV 和 MZ 两种存档格式。理由是压缩和 JsonEx 都来自 JavaScript，能延续已验证的实现并共用字段补丁逻辑；代价是安装包和内存比原生 GUI 大。Electron 的主进程负责文件访问和备份，渲染进程只负责界面，通过受限 preload IPC 通信，启用 contextIsolation 与 sandbox，关闭 renderer 的 Node integration。

模块划分：

1. GameProfile：发现引擎、数据库、启用插件和存档目录；支持手选 save 目录。
2. Codec：识别格式、大小限制、解压超时、损坏文件报错；MV 使用 LZString Base64，MZ 使用 pako 兼容的 zlib 二进制字符串存档格式，不能复用 MV 编解码假设。
3. RawDocument：原始树、元数据和引用索引；受限字段补丁及撤销/重做。
4. DatabaseIndex：名称、描述、图标索引、变量限制与物品规则。
5. SaveService：读取、差异预览、导出副本、覆盖备份和恢复。
6. GUI：搜索、分页/虚拟列表、字段编辑表单、结构树和存档对比。

界面从左到右：存档列表 → 总览/背包/变量/开关/角色/高级页 → 当前选中项说明与修改前后值。顶部显示游戏路径和引擎，底部显示待保存修改数量。变量和开关支持按 ID、日文原名、自定义中文别名搜索。原始 JSON 树默认只读，避免让用户直接破坏类型和引用。插件名称、元数据等仅在相关高级说明出现。

## 保存流程

选择存档 → 记录哈希 → 修改内存中的原始树 → 字段校验与差异预览 → 编码到同目录临时文件 → 回读并验证预期差异 → 检查源文件是否变化 → 备份原文件 → 替换目标文件。

默认“另存副本”；覆盖现有槽位时备份到独立备份目录，记录原路径、哈希、时间及补丁记录。游戏可能持续写入存档，检测到源文件变化应停止覆盖，保留待修改内容并让用户重新加载/合并。Windows 文件占用和替换失败要明确报错，保留原文件及备份；不能宣称多个文件一起写入是原子的。

现有槽位改金币通常无需更新 global.rpgsave。新增槽位、改头像或列表信息需要同步对应 global 条目，作为后续功能独立处理。config.rpgsave 不作为玩家进度存档修改。

## 实施阶段与验收

阶段 1：当前已完成编解码探针、数据库索引、7 个样本修改副本及报告。

阶段 2：可运行 GUI 最小版。选目录、列出存档、查看数据库名称、编辑金币/已有物品数量/有明确规则的变量和开关、撤销、导出副本；展示全部未知字段，禁止修改元数据。交付 Windows 便携 exe。

阶段 3：完善变量限制、物品上限、备份恢复、哈希冲突检测、两存档差异比较。测试损坏文件、日文路径、null 与包装数组、循环引用标记、字符串变量、缺失值、大存档及写入中断。

阶段 4：在游戏实际加载导出的测试存档，确认金币/物品/变量变化，继续游戏并再保存，再次解析新存档。此步骤通过后才能标为游戏内验证兼容。等级/经验/技能联动、装备修改和剧情预设仍需另行适配。

## 复现与产物

在本目录运行：

```powershell
node .\probe.cjs 'C:\Users\YuxuanLiu\Downloads\otomi-games.com_GN6UASU1O\魔法少女シルフィーナ\www'
```

输出到 output：report.json 为测试报告；database-index.json 为本地数据库；*.rpgsave.json 为可阅读的完整存档；*.gold-plus-one.rpgsave 为实验副本。测试副本未复制回游戏目录。第一阶段未安装完整 Electron GUI；第一阶段交付是解析验证和实现方案。后续 GUI 实现与当前使用方式见同目录“使用说明.md”。

参考：引擎实现以本地 js/rpg_core.js 的 JsonEx（约 8986 行起）、js/rpg_managers.js 的 StorageManager 为准。[MV 核心源码项目](https://github.com/rpgtkoolmv/corescript)、[LZString 上游](https://github.com/pieroxy/lz-string)、[Electron 官方进程安全配置](https://www.electronjs.org/docs/latest/tutorial/security)。
