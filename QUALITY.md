# MVP 质量验收

本次实现仅在 app 子目录开发；游戏目录作为只读输入。

负责人独立验收：

- 构建和类型检查通过；内置压缩库与 7 个真实存档的游戏原版编码保持兼容。
- 编辑金币、库存、变量和开关只改变目标字段；66 个以上实际引用标记和数组包装保持原样。
- 主进程验证字段 ID、类型和范围；拒绝任意路径和元数据修改。
- 导出使用独占创建，拒绝覆盖已存在文件；读回验证；原始文件哈希不变。
- 变量限制生效；字符串变量不被数字化；不因显示缺失值而填充整个数组。
- GUI 实际启动，覆盖选游戏、读档、搜索、编辑、撤销、预览和导出路径。
- 便携 Windows 构建产物可定位；记录尚未通过的验收项，不将格式测试称为游戏内兼容验收。

setItemMax 复核修正：本游戏的 villaA_itemIdMaxArray 是插件全局变量，没有该插件自带的存档持久化逻辑。数据库备注/默认上限可推导；运行时覆盖不能从存档可靠恢复。不能把运行时覆盖描述为已从存档读取。

## 2026-09-12 验收结果

- 主进程与 React TypeScript 检查、Vite 生产构建通过；7 个单元测试通过。
- 9 个真实文件的生产编码器与原始编码内容完全一致。
- 7 个真实游戏存档分别通过金币修改、导出回读、元数据完整性、拒绝覆盖、撤销、非法输入拒绝；额外验证物品、字符串变量和开关修改。
- 独立临时游戏副本验证源文件发生变化时拒绝导出，不生成目标文件。
- Playwright 启动实际 Electron，模拟原生目录选择返回值，通过真实 UI 选择 file1、输入金币、检查草稿阻止导出、应用、导出、回读金币、撤销、搜索变量。源代码构建和便携 exe 均通过。
- 已查看总览与变量页截图，没有文字遮挡。截图在 output/gui-overview.png 和 gui-variables.png。
- 便携版为 release/latest.txt 指向的目录，约 386 MB；必须保留运行库和 resources，不可单独移动 exe。
- 尚未做游戏内读档和继续游玩的验收；没有覆盖原存档，没有增加 global 槽位。

审查修复了新版 LZString 与旧版 Base64 尾部不兼容、任意 kind 回落到防具修改、无界开关 ID、导出期间数据快照变化、写失败残留副本，以及未提交输入导出时序问题。使用了明确的字段白名单、原始树补丁、主进程校验、独占创建、回读验证、源文件哈希、worker 超时与内存限制。

Sol low 子代理完成了主体实现和部分修订，随后触及额度限制；主代理接手完成其余修复、源码格式化、独立验证及打包。

## v0.2 角色属性与搜索

13 个单元测试与构建通过。验证八项属性通过目标基础值反算永久加成、降低属性产生负加成、当前 HP/MP、非法字段和缺失数据库拒绝、引用元数据保留；新增 key/名称/ID 片段匹配、大小写及全角归一化、未命名识别测试。7 个真实存档额外通过攻击修改/导出/撤销。GUI 验证角色攻击设为 200、导出回读、撤销、key 搜索与隐藏未命名开关选项。
