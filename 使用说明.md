# RPG Maker MV 存档实验室

这是 Windows 桌面修改器 v0.2，已针对《魔法少女シルフィーナ》的 RPG Maker MV 1.6.1 存档验证编解码。无需安装 Node.js，便携文件夹内双击 RPGMakerSaveLab.exe。

## 使用

1. 保持整个便携文件夹完整，双击 RPGMakerSaveLab.exe。
2. 点击“选择游戏目录”，选择：
   `C:\Users\YuxuanLiu\Downloads\otomi-games.com_GN6UASU1O\魔法少女シルフィーナ\www`
3. 左侧选择存档。总览可改金币；背包可改已有物品数量；角色页可以改当前 HP/MP 和八项基础属性。背包、变量、开关支持按 ID、名称和 key 片段搜索；空格分隔多个关键词会同时匹配。开关页默认隐藏未命名项，可取消勾选；该偏好会保留。
4. 修改输入后点击应用，右侧查看修改前后差异。需要恢复上一步时点击撤销。
5. 点击导出副本，选择游戏 save 目录之外的一个新文件。默认输出到文档目录下 RPGMaker Save Lab。已存在的文件不会被覆盖。

导出操作本身不改动游戏目录。需要在游戏中使用时，先退出游戏并另外备份原槽位，然后将导出文件复制到对应 save 槽位并使用原文件名，例如 file1.rpgsave。首版不提供新增存档槽位或 global.rpgsave 同步；不要把副本改成一个全新槽位并期望它自动显示在读档菜单。

## 已支持与边界

- 金币、已有背包条目、已有数字/字符串变量、开关；数据库名称显示、搜索、修改差异、撤销、导出副本。
- 角色可修改当前 HP/MP，以及最大 HP/MP、攻击、防御、法攻、法防、敏捷和幸运的基础值。基础值指当前等级成长值加永久加成，应用时只改存档的永久加成。装备、状态和插件倍率仍由游戏另算，所以游戏面板最终数值可能不同。HP/MP 超过游戏实际最大值时可能被限制，改 HP 不自动清除死亡状态。
- 角色等级、地图和完整 JSON 仅查看，不改等级、装备和引用结构。
- VariableLimitation 中的已知范围在写入时校验。变量名称可能只是显示缓存，不能保证改某个同名变量就改变角色真实属性。
- 物品备注上限和默认值可以读取；游戏运行时插件命令覆盖不一定保存在存档内，无法可靠推导。
- 只验证本游戏 MV；MZ、VX Ace 或其他自定义存档格式不属于已验证支持范围。
- 完成真实文件编解码、字段保留、导出回读和 GUI 测试；尚未在游戏内加载并继续游玩验证。

## 开发

app 为独立 Electron + TypeScript + React 项目。安装依赖后运行 `pnpm build`、`pnpm test`，通过 `pnpm start` 启动编译版。源码不包含游戏数据库和存档，实验输出已被 gitignore 排除。

测试脚本：probe.cjs 为原游戏编解码基线；verify-real-codec.cjs 为库兼容检查；verify-app.cjs 为真实存档服务集成检查；gui-smoke.cjs 为 Electron UI 自动化。后几个脚本针对当前工作环境和本地样本，需要先运行 probe.cjs。package-portable.ps1 打包 Windows 便携文件夹，release/latest.txt 记录最新产物目录。
